<Window x:Class="PlannerView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlannerView"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        x:Name="Window"
        Title="MainWindow" Height="600" Width="1400" WindowStartupLocation="CenterScreen" SizeToContent="WidthAndHeight" FontFamily="Roboto" ResizeMode="CanResizeWithGrip" WindowStyle="ThreeDBorderWindow">
    <Window.Resources>
        <ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBox.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style BasedOn="{StaticResource MaterialDesignListBoxItem}"
                   TargetType="{x:Type ListBoxItem}">
                <Setter Property="Height" Value="50"></Setter>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="FontWeight" Value="Bold" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Visibility="Collapsed" x:Name="Wrap" Grid.ColumnSpan="2"  Panel.ZIndex="1000" Margin="0,0,-24.8,0.4">
            <Button
                x:Name="WrapBtn"
                Opacity="0.4"
                Background="Black" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Width="1395" 
                Height="570" Margin="0,0,11.4,0" BorderBrush="Black"
                Click="WrapBtn_OnClick"/>
        </Grid>
        <!--Меню-->
        <Grid Grid.Column="0" Width="280" Background="AntiqueWhite">
            <ListBox x:Name="Menu" 
                     Grid.Column="0" 
                     SelectionMode="Single" 
                     SelectionChanged="ListBox_SelectionChanged">
                <ListBoxItem>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CalendarSync" Foreground="#383838" Margin="0 4"/>
                        <Label FontSize="14">Бессрочные задачи</Label>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem IsSelected="True">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CalendarToday" Foreground="#383838"  Margin="0 4"/>
                        <Label FontSize="14">Задачи на сегодня</Label>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CalendarArrowRight" Foreground="#383838" Margin="0 4"/>
                        <Label FontSize="14">Предстоящие задачи</Label>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CalendarCheck" Foreground="#383838"  Margin="0 4"/>
                        <Label FontSize="14">Выполненные задачи</Label>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CalendarRemove" Foreground="#383838"  Margin="0 4"/>
                        <Label FontSize="14">Просроченные задачи</Label>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem Background="#FF6B6B">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CalendarStar" Foreground="#383838"  Margin="0 4"/>
                        <Label FontSize="14">Задачи срочного приоритета</Label>
                    </StackPanel>
                </ListBoxItem>
            </ListBox>
        </Grid>
        <!--Планировщик-->
        <Grid Grid.Column="1"  x:Name="GridMain" HorizontalAlignment="Right" VerticalAlignment="Top" Height="540" Width="1100">
            <Label x:Name="TaskTitle" FontSize="24"
                   FontWeight="Bold">Задачи на сегодня</Label>
            <Button x:Name="AddTaskBtn" 
                    Content="Добавить задачу" 
                    Background="#FF6B6B"
                    BorderBrush="#FF6B6B"
                    Width="156"
                    Height="37" Click="AddTaskBtn_Click" Margin="934,10,10,493"/>
            <Button x:Name="AddCategoryBtn" 
                    Content="Добавить категорию" 
                    Background="#FF6B6B"
                    BorderBrush="#FF6B6B"
                    Height="37"
                    Click="AddCategoryBtn_Click" Margin="717,10,205,493"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,57,0,0">
                <StackPanel x:Name="TaskList" 
                        Orientation="Vertical"
                        HorizontalAlignment="Left"
                        CanVerticallyScroll="True">
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!--<Grid Height="30" VerticalAlignment="Top" Background="#666666">
            <Grid.Effect>
                <DropShadowEffect BlurRadius="1" ShadowDepth="1" Color="#FFACACAC"/>
            </Grid.Effect>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                -->
        <!--<materialDesign:PopupBox PlacementMode="BottomAndAlignRightEdges" StaysOpen="True" Margin="10">
                    <StackPanel Width="150">
                        <Button Content="Settings"/>
                        <Button Content="Account"/>
                        <Button Content="Help"/>
                        <Separator/>
                        <Button Content="Logout"/>
                    </StackPanel>
                </materialDesign:PopupBox>-->
        <!--
            </StackPanel>
            <TextBlock Text="Planner" VerticalAlignment="Bottom" FontSize="16" Margin="0,0,0,4.4"/>
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Foreground="#FFFFFF" FontFamily="Roboto">Планировщик задач</TextBlock>
        </Grid>-->
    </Grid>
</Window> 
 