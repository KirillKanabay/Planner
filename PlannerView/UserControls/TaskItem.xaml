<UserControl x:Class="PlannerView.TaskItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlannerView"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" Width="1104.962" Height="68.321" Background="White">
    <Grid Margin="0 0 0 3">
        <materialDesign:Card>
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Height="36">
                <TextBlock x:Name="TaskName" Width="195" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5
                           ,10"><Run Text="Название задачи"/></TextBlock>
                <Label x:Name="StartDate" Margin="10,6" Width="101" VerticalAlignment="Center" BorderThickness="0" Content="29.10.2020, 19:00"/>
                <Border Background="#15C651" CornerRadius="5" Height="25">
                    <Label x:Name="EndDate" Foreground="#FFFFFF" Width="104" Content="29.10.2020, 19:00" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                </Border>
                <Border x:Name="PriorityBackground" Background="#15C651" CornerRadius="5" Height="25" Margin="5 0">
                    <Label x:Name="Priority" Foreground="#fff" Content="Без приоритета" Width="153" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                </Border>
                <Border x:Name="CategoryBackground" Background="#15C651" CornerRadius="5" Height="25" Margin="5 0">
                    <Label x:Name="Category" Foreground="#FFF" Content="Название категории" Width="306" HorizontalContentAlignment="Center" VerticalAlignment="Center" BorderThickness="0"/>
                </Border>
                <Button 
                    x:Name="FinishTaskBtn"
                    Content="Завершить задачу" 
                    Background="#FF15C651" 
                    Width="156" 
                    Height="Auto" 
                    HorizontalAlignment="Right" 
                    BorderBrush="#FF15C651"
                    VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Click="Button_Click" Margin="5 5"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
